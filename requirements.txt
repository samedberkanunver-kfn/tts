# PyTorch with CUDA support
# Install separately: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchaudio>=2.0.0

# NumPy (pinned for compatibility)
numpy==1.26.4

# Transformers & PEFT (pinned versions)
transformers==4.40.0
peft==0.10.0

# Dataset loading (pinned version - newer versions require torchcodec)
datasets==2.18.0
huggingface_hub

# Audio processing
soundfile
librosa
audioread

# Phonemization (requires espeak-ng system dependency)
# Linux: sudo apt-get install espeak-ng
# macOS: brew install espeak-ng
phonemizer>=3.2.0

# Training utilities
tqdm>=4.65.0
tensorboard>=2.13.0

# Optional
scipy>=1.10.0
pyyaml>=6.0
