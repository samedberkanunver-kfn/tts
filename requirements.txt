# PyTorch with MPS support (Apple Silicon)
# Install from: https://pytorch.org/get-started/locally/
torch>=2.0.0
torchaudio>=2.0.0

# Hugging Face ecosystem
transformers>=4.30.0
datasets>=2.14.0
peft>=0.5.0
accelerate>=0.20.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0

# Phonemization (requires espeak-ng system dependency)
# Install espeak-ng first: brew install espeak-ng
phonemizer>=3.2.0

# Text processing
sentencepiece>=0.1.99

# Utilities
numpy>=1.24.0
einops>=0.6.0
matplotlib>=3.7.0
tqdm>=4.65.0
pyyaml>=6.0

# Logging and monitoring
tensorboard>=2.13.0
wandb>=0.15.0  # Optional: for advanced experiment tracking

# Development tools
ipython>=8.14.0
jupyter>=1.0.0

# Note: After installing requirements, run:
# brew install espeak-ng
